spring:
  config:
    activate:
      on-profile: dev

  datasource:
    url: ${SPRING_DATASOURCE_URL:jdbc:mysql://${DB_HOST:127.0.0.1}:${DB_PORT:3306}/${DB_NAME:proposal_governance}?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC}
    username: ${SPRING_DATASOURCE_USERNAME:dev_user}
    password: ${SPRING_DATASOURCE_PASSWORD:Atharv@2002}

  jpa:
    hibernate:
      naming:
        physical-strategy: org.hibernate.boot.model.naming.PhysicalNamingStrategyStandardImpl
    show-sql: true
    properties:
      hibernate:
        format_sql: true

razorpay:
  key_id: ${RAZORPAY_KEY_ID:rzp_test_TMGUnzZTkKHWLG}
  key_secret: ${RAZORPAY_KEY_SECRET:8pJ7ROBcDd6IYnLhaiTf8u41}

logging:
  level:
    root: INFO
    com.innovaura: DEBUG
    org.springframework.security: DEBUG
